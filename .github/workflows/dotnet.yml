# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    uses: kube-the-home/github-actions/.github/workflows/dotnet-build-test.yaml@a742a2eda2b622e6902ee5e97db2fcd1d6c432a9 # v1.4.7
