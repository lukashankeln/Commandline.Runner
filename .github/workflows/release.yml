name: Release to NuGet

on:
  release:
    types: [published]

jobs:
  release:
    uses: kube-the-home/github-actions/.github/workflows/dotnet-nuget-release.yaml@70b35bb255a89f5a9281934be8941da086031276 # v1.4.11
    secrets:
      nuget_api_key: ${{ secrets.nuget_api_key }}
