name: Release to NuGet

on:
  release:
    types: [published]

jobs:
  release:
    uses: kube-the-home/github-actions/.github/workflows/dotnet-nuget-release.yaml@6a42c4b92ae30747603d0846aad29a463f0ea11e # v1.4.3
    secrets:
      nuget_api_key: ${{ secrets.nuget_api_key }}
