name: Release to NuGet

on:
  release:
    types: [published]

jobs:
  release:
    uses: kube-the-home/github-actions/.github/workflows/dotnet-nuget-release.yaml@9a4771645c64b73a0f4c5409e3c3b714932590f2 # v1.4.8
    secrets:
      nuget_api_key: ${{ secrets.nuget_api_key }}
