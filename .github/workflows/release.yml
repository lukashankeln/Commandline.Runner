name: Release to NuGet

on:
  release:
    types: [published]

jobs:
  release:
    uses: kube-the-home/github-actions/.github/workflows/dotnet-nuget-release.yaml@a4e300db6adb24b0f86b3fee25548b76dd18f999 # v1.4.4
    secrets:
      nuget_api_key: ${{ secrets.nuget_api_key }}
