name: Release to NuGet

on:
  release:
    types: [published]

jobs:
  release:
    uses: kube-the-home/github-actions/.github/workflows/dotnet-nuget-release.yaml@b67640d904b560a3a20f5cb94e6ddd5bfa955d65 # v1.4.10
    secrets:
      nuget_api_key: ${{ secrets.nuget_api_key }}
