name: Release to NuGet

on:
  release:
    types: [published]

jobs:
  release:
    uses: kube-the-home/github-actions/.github/workflows/dotnet-nuget-release.yaml@a742a2eda2b622e6902ee5e97db2fcd1d6c432a9 # v1.4.7
    secrets:
      nuget_api_key: ${{ secrets.nuget_api_key }}
