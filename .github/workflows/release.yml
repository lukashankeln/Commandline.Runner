name: Release to NuGet

on:
  release:
    types: [published]

jobs:
  release:
    uses: kube-the-home/github-actions/.github/workflows/dotnet-nuget-release.yaml@5b44e7a71b25dff53182e911f7708c62ac20fd3a # v1.2.0
    secrets:
      nuget_api_key: ${{ secrets.nuget_api_key }}
