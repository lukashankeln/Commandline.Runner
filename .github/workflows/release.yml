name: Release to NuGet

on:
  release:
    types: [published]

jobs:
  release:
    uses: kube-the-home/github-actions/.github/workflows/dotnet-nuget-release.yaml@962260b6048afd17325015c372eeb835a04b25b2 # v1.4.2
    secrets:
      nuget_api_key: ${{ secrets.nuget_api_key }}
