name: Release to NuGet

on:
  release:
    types: [published]

jobs:
  release:
    uses: kube-the-home/github-actions/.github/workflows/dotnet-nuget-release.yaml@7d8043b1d454e713b465c324c99d929a4a625a98 # v1.4.12
    secrets:
      nuget_api_key: ${{ secrets.nuget_api_key }}
