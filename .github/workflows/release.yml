name: Release to NuGet

on:
  release:
    types: [published]

jobs:
  release:
    uses: kube-the-home/github-actions/.github/workflows/dotnet-nuget-release.yaml@cc715d5c26c3498c970eb730b01344aa61c5152d # v1.3.4
    secrets:
      nuget_api_key: ${{ secrets.nuget_api_key }}
