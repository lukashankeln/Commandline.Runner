name: Release to NuGet

on:
  release:
    types: [published]

jobs:
  release:
    uses: kube-the-home/github-actions/.github/workflows/dotnet-nuget-release.yaml@e698429ae3d6096c14d7eb6d2ffef1da8c26cdea # v1.4.1
    secrets:
      nuget_api_key: ${{ secrets.nuget_api_key }}
