name: Release to NuGet

on:
  release:
    types: [published]

jobs:
  release:
    uses: kube-the-home/github-actions/.github/workflows/dotnet-nuget-release.yaml@2b44df6e69f5674627ef7a7b3c0ddd752781e6f3 # v1.4.6
    secrets:
      nuget_api_key: ${{ secrets.nuget_api_key }}
